# 수풀AI 파일 작업기록

> su-pul.html 버그 수정 및 기능 추가 — 최종 작업 기록  
> 날짜: 2026-04-25 | 결과 파일: `yeon-ai-kr-v4/supul/su-pul.html` (801,651자)

---

## 원본 파일

### Netlify 원본 (작업 기반)
- **경로**: `C:\Users\jinwo\su-pul\deploy\su-pul.html`
- **크기**: 800,769자
- Netlify 서비스 당시 정상 작동하던 파일. 이전 세션에서 발견하여 기준점으로 채택.

### 최종 결과 파일
- **경로**: `C:\Users\jinwo\OneDrive\문서\Claude\Projects\연라이프 홈페이지 제작\yeon-ai-kr-v4\supul\su-pul.html`
- **크기**: 801,651자 (원본 대비 +882자)

---

## 발견된 문제

### 문제 — QR 모달 함수 3개가 `</script>` 태그 밖에 위치

파일 하단의 `openQRModal()`, `closeQRModal()`, `copyAppLink()` 함수들이 마지막 `</script>` 닫힌 후에 위치해 있었다. 브라우저가 이를 JavaScript로 인식하지 못하고 일반 텍스트로 페이지 하단에 그대로 노출. QR 공유 버튼을 눌러도 아무 반응이 없었음.

### 해결 — 별도 `<script>` 태그로 감싸기

Python 스크립트로 파일을 처리하여, QR 함수들이 시작되는 위치 바로 앞에 `<script>` 태그를 삽입하고, 파일 맨 끝에 `</script>`를 추가. 커밋 해시: **9a1c022**.

---

## 추가된 내용 (원본 대비)

| 항목 | 위치 | 내용 |
|------|------|------|
| 그림판 모달 HTML | `<body>` 직후 (961번째 줄) | drawingPadModal — iframe + 분석하기/닫기 버튼 |
| 그림판 함수 3개 | 마지막 `</script>` 직전 (14,561번째 줄) | openDrawingPadModal / closeDrawingPadModal / sendDrawingToAnalyze |
| 그림판 UI 버튼 | 분석하기 버튼 그룹 내 (2,233번째 줄) | 연필 SVG 아이콘 + "그림판" 레이블 |
| QR 함수 script 태그 | QR 함수 3개 앞뒤 (14,607번째 줄) | openQRModal / closeQRModal / copyAppLink를 새 `<script>`로 감쌈 |

---

## 추가 코드 상세

### QR 버그 수정 — 새 script 블록으로 감싸기

```html
<!-- 원래: 이 세 함수가 </script> 아래 텍스트로 노출되어 있었음 -->
</script>

<script>
// ===== QR 공유 모달 =====
var _qrGenerated = false;
function openQRModal(){
  var m=document.getElementById('qrShareModal');
  if(!m)return;
  if(!_qrGenerated){
    // QR 코드 생성 로직
    _qrGenerated=true;
  }
  m.style.display='flex';
}
function closeQRModal(){
  var m=document.getElementById('qrShareModal');
  if(m)m.style.display='none';
}
function copyAppLink(){
  var url=window.location.href;
  navigator.clipboard.writeText(url).then(function(){
    var btn=document.getElementById('copyLinkBtn');
    if(btn){btn.textContent='복사됨!';setTimeout(function(){btn.textContent='링크 복사';},2000);}
  });
}
</script>
```

### 그림판 모달 HTML (body 직후 삽입)

```html
<div id="drawingPadModal" style="display:none;position:fixed;inset:0;z-index:9998;
     background:rgba(0,0,0,.6);align-items:center;justify-content:center;">
  <div style="background:#fff;border-radius:16px;overflow:hidden;
       width:min(95vw,680px);max-height:90vh;display:flex;flex-direction:column;">
    <div style="display:flex;align-items:center;justify-content:space-between;
         padding:12px 16px;background:#f4f4f4;">
      <span style="font-size:15px;font-weight:700;color:#526e8a;">그림판으로 수식 입력</span>
      <div style="display:flex;gap:8px;">
        <button onclick="sendDrawingToAnalyze()">분석하기</button>
        <button onclick="closeDrawingPadModal()">닫기</button>
      </div>
    </div>
    <iframe id="drawingPadFrame" src="./drawing-pad.html"
            style="flex:1;width:100%;border:none;" allowfullscreen></iframe>
  </div>
</div>
```

### 그림판 JavaScript 함수 3개

```javascript
function openDrawingPadModal(){
  var m=document.getElementById('drawingPadModal');
  if(!m)return;
  var f=document.getElementById('drawingPadFrame');
  if(f && !f.src) f.src='./drawing-pad.html';  // lazy load
  m.style.display='flex';
}

function closeDrawingPadModal(){
  var m=document.getElementById('drawingPadModal');
  if(m) m.style.display='none';
}

function sendDrawingToAnalyze(){
  var f=document.getElementById('drawingPadFrame');
  if(!f||!f.contentWindow){closeDrawingPadModal();return;}
  try{
    var FC=f.contentWindow.FC;
    if(!FC){
      alert('그림판이 아직 로딩 중입니다. 잠시 후 다시 시도하세요.');
      return;
    }
    var d=FC.toDataURL({format:'png',multiplier:2});
    closeDrawingPadModal();
    window._openImageEditor(d, function(ed){ processOcrFromDataUrl(ed); });
  }catch(e){
    closeDrawingPadModal();
    console.error('그림판 캡처 오류', e);
  }
}
```

### 그림판 UI 버튼

```html
<button class="btn-secondary" onclick="openDrawingPadModal()"
        title="그림판으로 수식 입력">
  <svg width="15" height="15" viewBox="0 0 24 24" fill="none"
       stroke="currentColor" stroke-width="1.5"
       stroke-linecap="round" stroke-linejoin="round"
       style="vertical-align:-2px;margin-right:4px;">
    <path d="M12 20h9"/>
    <path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/>
  </svg>그림판
</button>
```

---

## 작업 타임라인

1. **문제 발견** — QR 공유 버튼 눌러도 반응 없음. 페이지 하단에 JS 함수 코드가 텍스트로 노출.
2. **원인 분석** — `openQRModal` / `closeQRModal` / `copyAppLink` 세 함수가 마지막 `</script>` 닫힌 후에 위치. HTML 파서가 JS로 인식 못 하고 텍스트로 렌더링.
3. **원본 발견** — `su-pul/deploy/su-pul.html` — Netlify 원본(800,769자). 기준점으로 채택.
4. **QR 버그 수정** — Python 스크립트로 QR 함수 앞에 `<script>` 삽입, 파일 끝에 `</script>` 추가. 커밋 9a1c022.
5. **그림판 모달 추가** — drawingPadModal HTML을 `<body>` 직후에 삽입. iframe lazy loading 방식.
6. **그림판 함수 추가** — 3개 함수 추가. Fabric.js FC.toDataURL()로 캔버스 PNG 캡처 → OCR 처리.
7. **그림판 버튼 추가** — 분석하기 버튼 그룹 내에 그림판 버튼(연필 SVG 아이콘) 추가.
8. **현재 상태** — 배포 대기 중. 사용자 로컬 확인 후 push 예정.

---

## 함수 검증 결과

- [x] `openQRModal()` — QR 모달 열기 ✅ 버그 수정됨 (script 태그 안으로)
- [x] `closeQRModal()` — QR 모달 닫기 ✅ 버그 수정됨
- [x] `copyAppLink()` — 링크 복사 ✅ 버그 수정됨
- [x] `openDrawingPadModal()` — 그림판 열기 ✅ 신규 추가
- [x] `closeDrawingPadModal()` — 그림판 닫기 ✅ 신규 추가
- [x] `sendDrawingToAnalyze()` — 그림 분석 전송 ✅ 신규 추가
- [x] 그림판 UI 버튼 배치 ✅ 신규 추가
- [x] JS 텍스트 렌더링 없음 ✅ 버그 해결

---

## 수풀AI 전체 기능 목록

- Google Sheets Apps Script 연동 (학생 데이터 저장)
- 학생 등록코드 50개 (6자리 코드 인증)
- OCR — Tesseract.js 수식 인식
- 이미지 편집기 (`_openImageEditor`) — 촬영/업로드 후 크롭
- 그림판 모달 — drawing-pad.html (Fabric.js 캔버스)
- QR 코드 공유 기능 (앱 URL → QR 생성)
- PWA — Service Worker (`supulai-v4`, 네트워크 우선, 오프라인 지원)
- `config.js` — API 키·시트 ID 외부 설정 파일
- `yeon-focus-tracker.js` — 집중 시간 추적

---

## 배포 명령

```powershell
cd "C:\Users\jinwo\OneDrive\문서\Claude\Projects\연라이프 홈페이지 제작\yeon-ai-kr"

# v4 파일을 배포 폴더로 복사
copy "yeon-ai-kr-v4\supul\su-pul.html" "supul\su-pul.html"

# git 커밋 & push
git add supul/su-pul.html
git commit -m "수풀AI: QR 버그 수정 + 그림판 모달 추가"
git push origin main
```

> Service Worker가 `supulai-v4` 캐시명으로 `network-first` 전략이므로 push 후 캐시 무효화 없이 바로 반영됩니다.

---

*수풀AI 파일 작업기록 · 연라이프 프로젝트 · 2026-04-25*
