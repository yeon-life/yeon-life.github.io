# 연라이프 구독 백엔드 — 신청 접수 + 1클릭 승인 + 문자 자동매칭 (무료)

Google Apps Script 하나로 ① 구독 신청을 시트에 저장 ② 연소사님께 메일 알림
③ 관리자 화면에서 **1클릭 승인** ④ (선택) **입금 문자 자동 승인**까지 됩니다.
PG 계약·서버·빌링 전부 불필요. **한 번만 배포**(약 3분).

## 배포 순서
1. [Google Apps Script](https://script.google.com) → **새 프로젝트**
2. 아래 코드 전체 붙여넣기 → `ADMIN_KEY`를 **길고 아무도 모르는 암호**로 바꾸기
3. **배포 → 새 배포 → 웹 앱** · 실행=**나** · 액세스=**모든 사용자** → 배포 → 권한 승인
4. 나온 **웹 앱 URL**(…/exec) 복사
5. `구독.html`의 `PAY.scriptUrl` + 관리자 화면(`관리자_구독승인.html`)에 그 URL과 `ADMIN_KEY` 입력 → 끝

## 코드 (Code.gs)
```javascript
/** 연라이프 구독 백엔드 — 신청 + 1클릭 승인 + (선택)문자 자동매칭 */
var ADMIN_EMAIL = 'jinwooshon@gmail.com';
var ADMIN_KEY   = 'CHANGE-ME-긴암호-예-yeon2026x9q7';   // ⚠ 관리자 접근 키. 공개 금지(여기 Apps Script에만).
var SHEET_NAME  = '구독신청';
var HEAD = ['신청시각','이름','연락처','입금자명','결제방법','금액(월)','메모','상태','처리시각'];

function sheet_(){
  var ss=SpreadsheetApp.getActiveSpreadsheet();
  var sh=ss.getSheetByName(SHEET_NAME)||ss.insertSheet(SHEET_NAME);
  if(sh.getLastRow()===0) sh.appendRow(HEAD);
  return sh;
}
function out_(o,e){
  var s=JSON.stringify(o);
  if(e&&e.parameter&&e.parameter.callback)   // JSONP (관리자 화면 CORS 회피)
    return ContentService.createTextOutput(e.parameter.callback+'('+s+')').setMimeType(ContentService.MimeType.JAVASCRIPT);
  return ContentService.createTextOutput(s).setMimeType(ContentService.MimeType.JSON);
}

function doPost(e){
  try{
    var d=JSON.parse(e.postData.contents||'{}');
    if(d.action==='approve') return (d.key===ADMIN_KEY)? setStatus_(d.row,'승인') : out_({ok:false,err:'auth'});
    if(d.action==='reject')  return (d.key===ADMIN_KEY)? setStatus_(d.row,'반려') : out_({ok:false,err:'auth'});
    if(d.action==='sms')     return (d.key===ADMIN_KEY)? smsMatch_(d.text) : out_({ok:false,err:'auth'});
    return intake_(d);   // 일반 구독 신청
  }catch(err){ return out_({ok:false,err:String(err)}); }
}

function doGet(e){
  if((e.parameter.key||'')!==ADMIN_KEY) return out_({ok:false,err:'auth'}, e);
  var sh=sheet_(), rows=sh.getDataRange().getValues(), list=[];
  for(var i=1;i<rows.length;i++){
    list.push({row:i+1, at:String(rows[i][0]), name:rows[i][1], contact:rows[i][2], payer:rows[i][3],
               method:rows[i][4], price:rows[i][5], memo:rows[i][6], status:rows[i][7]||'대기'});
  }
  return out_({ok:true, list:list.reverse()}, e);
}

function intake_(d){
  sheet_().appendRow([new Date(), d.name, d.contact, d.payer, d.method, d.price, d.memo, '대기', '']);
  MailApp.sendEmail(ADMIN_EMAIL, '[연라이프 구독 신청] '+d.name,
    '이름: '+d.name+'\n연락처: '+d.contact+'\n입금자명: '+d.payer+'\n결제방법: '+d.method+
    '\n금액: 월 '+d.price+'원\n메모: '+(d.memo||'')+'\n\n관리자 화면에서 [승인]을 누르세요.');
  return out_({ok:true});
}

function setStatus_(row, status){
  var sh=sheet_();
  sh.getRange(row,8).setValue(status);
  sh.getRange(row,9).setValue(new Date());
  var contact=String(sh.getRange(row,3).getValue());   // 연락처
  if(status==='승인' && contact.indexOf('@')>=0){
    MailApp.sendEmail(contact, '[연라이프] 구독이 열렸어요 🌿',
      '입금이 확인되어 결 시리즈 구독이 시작되었습니다. 고맙습니다.\nhttps://y-life.kr');
  }
  return out_({ok:true,status:status});
}

/** (선택) 입금 알림 문자 자동 매칭 — 폰에서 SMS를 이 액션으로 보내면 입금자명이 맞으면 자동 승인 */
function smsMatch_(text){
  var sh=sheet_(), rows=sh.getDataRange().getValues();
  for(var i=rows.length-1;i>=1;i--){
    if((rows[i][7]||'대기')!=='대기') continue;
    var payer=String(rows[i][3]||'').replace(/\s/g,'');
    if(payer && text.replace(/\s/g,'').indexOf(payer)>=0) return setStatus_(i+1,'승인');
  }
  return out_({ok:false,err:'no-match'});
}
```

## 🅑 입금 문자 자동 승인 (선택 · 안드로이드)
1. 안드로이드에 **MacroDroid**(무료) 설치
2. 트리거: **SMS 수신**(발신=은행) → 동작: **HTTP POST** to 웹앱URL,
   본문 `{"action":"sms","key":"위 ADMIN_KEY","text":"{sms_message}"}`
3. 입금 문자에 **입금자명**이 있으면, 대기 중 신청과 자동 매칭되어 **자동 승인**됩니다.
   (이름이 같은 사람이 여러 명이면 1클릭 승인으로 처리 — 안전장치)

## 보안 메모
- 카드번호·비밀번호·비밀키 없음. `ADMIN_KEY`는 **공개 파일에 절대 넣지 말 것**(Apps Script와 관리자 화면 입력창에만).
- 관리자 화면은 키 없이는 아무 데이터도 못 봅니다(서버에서 차단).
