# 연라이프 구독 — 무료 백엔드(Google Apps Script) 가이드

구독 신청을 **구글 시트에 자동 저장 + 연소사님께 메일 알림**해 주는 무료 백엔드입니다.
PG 계약·서버·빌링 전부 필요 없습니다. 한 번만 배포하면 됩니다(약 3분).

> 이 단계 없이도 `구독.html`은 **메일 폴백**으로 바로 작동합니다(신청 시 연소사님께 메일 발송).
> 아래는 신청을 **시트에 차곡차곡 모으고 1클릭 승인 관리**하고 싶을 때만 하면 됩니다.

## 배포 순서
1. [Google Apps Script](https://script.google.com) 접속 → **새 프로젝트**
2. 아래 코드 전체를 붙여넣기(기존 내용 삭제)
3. 상단 **배포 → 새 배포 → 유형: 웹 앱**
   - 실행 계정: **나(본인)**
   - 액세스 권한: **모든 사용자**
4. **배포** → 권한 승인 → 나오는 **웹 앱 URL**(…/exec) 복사
5. `구독.html`의 `PAY.scriptUrl` 에 그 URL 붙여넣기 → 끝

## 코드 (Code.gs)
```javascript
// 연라이프 구독 신청 접수 백엔드
var ADMIN_EMAIL = 'jinwooshon@gmail.com';   // 알림 받을 메일
var SHEET_NAME  = '구독신청';

function doPost(e){
  try{
    var d = JSON.parse(e.postData.contents);
    var ss = SpreadsheetApp.getActiveSpreadsheet();
    var sh = ss.getSheetByName(SHEET_NAME) || ss.insertSheet(SHEET_NAME);
    if(sh.getLastRow() === 0){
      sh.appendRow(['신청시각','이름','연락처','입금자명','결제방법','금액(월)','메모','승인','승인일']);
    }
    sh.appendRow([new Date(), d.name, d.contact, d.payer, d.method, d.price, d.memo, false, '']);

    MailApp.sendEmail(ADMIN_EMAIL,
      '[연라이프 구독 신청] ' + d.name,
      '이름: ' + d.name + '\n연락처: ' + d.contact + '\n입금자명: ' + d.payer +
      '\n결제방법: ' + d.method + '\n금액: 월 ' + d.price + '원\n메모: ' + (d.memo||'') +
      '\n\n입금 확인 후 시트의 [승인] 칸을 TRUE로 바꾸면 됩니다.');

    return ContentService.createTextOutput(JSON.stringify({ok:true})).setMimeType(ContentService.MimeType.JSON);
  }catch(err){
    return ContentService.createTextOutput(JSON.stringify({ok:false,err:String(err)})).setMimeType(ContentService.MimeType.JSON);
  }
}
```

## 1클릭 승인(관리)
- 신청이 들어오면 **구글 시트 `구독신청` 탭**에 한 줄씩 쌓입니다.
- 입금이 확인되면 그 줄 **[승인] 칸을 TRUE(체크)** 로 바꿉니다 = 승인 완료 표시.
- (다음 단계) 승인 시 자동으로 구독자 활성화/안내 메일 발송하도록 `onEdit` 트리거를 붙일 수 있습니다 — 원하시면 추가해 드립니다.

## 보안 메모
- 이 백엔드는 **계좌·신청정보만** 다룹니다. 카드번호·비밀번호·비밀키 없음.
- 멤버십 자동 활성화(잡지 잠금 해제)를 서버 검증으로 연결하는 건 다음 단계입니다.
