# 영풀 파일 작업기록

> yeong-pul.html 버그 수정 및 기능 추가 — 최종 작업 기록  
> 날짜: 2026-04-25 | 결과 파일: `yeon-ai-kr-v4/yeongpul/yeong-pul.html` (904,146자)

---

## 원본 파일

### Netlify 원본 (작업 기반)
- **경로**: `C:\Users\jinwo\yeon-teacher\deploy\app.html`
- **크기**: 901,111자
- Netlify 서비스 당시 정상 작동하던 파일. 모든 핵심 함수(applyTheme, resetSettings, printShareCard, openSettingsModal 등) 완전히 포함된 유일한 기준점.

### 최종 결과 파일
- **경로**: `C:\Users\jinwo\OneDrive\문서\Claude\Projects\연라이프 홈페이지 제작\yeon-ai-kr-v4\yeongpul\yeong-pul.html`
- **크기**: 904,146자 (원본 대비 +3,035자)

---

## 발견된 문제

### 문제 1 — GitHub 리포 파일 자체가 불완전한 버전
GitHub 리포의 yeong-pul.html은 Netlify 원본과 다른 파일이었다. applyTheme, resetSettings, printShareCard, openSettingsModal 등 핵심 함수들이 누락되어 버튼이 전혀 작동하지 않았다.

### 문제 2 — JS 코드가 텍스트로 렌더링됨
일부 파일에서 `</script>` 태그가 JS 문자열 내부에 unescaped 상태로 포함되어, 브라우저가 스크립트 블록을 중간에 닫아버렸다. 이후 코드가 일반 텍스트로 페이지 하단에 노출되었다.

### 문제 3 — 그림판 버튼 UI 누락
그림판 함수(openDrawingPadModal 등)가 정의되어 있었지만 UI에 버튼이 없어서 사용자가 그림판을 호출할 방법이 없었다.

---

## 해결 방법

### 해결 1 — deploy/app.html 원본으로 완전 교체
불완전한 GitHub 리포 파일을 버리고, Netlify 원본인 `deploy/app.html`을 기반으로 새로 작업. 모든 핵심 함수가 온전히 보존된 상태에서 기능 추가만 수행.

### 해결 2 — v3 동결, v4 작업본 분리
기존 작업 폴더를 `yeon-ai-kr-v3`로 동결(건드리지 않음)하고, `yeon-ai-kr-v4`를 새로운 작업본으로 생성. v4에서만 수정 진행.

---

## 추가된 내용 (원본 대비)

| 항목 | 위치 | 내용 |
|------|------|------|
| 학생 등록 코드 | 첫 번째 `const` 선언 직전 | 50개 코드 배열 `STUDENT_ENROLLMENT_CODES` |
| 그림판 모달 HTML | `<body>` 태그 직후 (807번째 줄) | 드로잉 패드 iframe + 닫기/전송 버튼 |
| 그림판 함수 3개 | 마지막 `</script>` 직전 (14,491번째 줄) | openDrawingPadModal / closeDrawingPadModal / sendDrawingToAnalyze |
| 그림판 UI 버튼 | 분석하기/샘플/지우기 버튼 그룹 뒤 (2,039번째 줄) | 연필 SVG 아이콘 + "그림판" 레이블 버튼 |

---

## 추가 코드 상세

### 그림판 모달 HTML (body 직후 삽입)

```html
<div id="drawingPadModal" style="display:none;position:fixed;inset:0;z-index:9998;
     background:rgba(0,0,0,.6);align-items:center;justify-content:center;">
  <div style="background:#fff;border-radius:16px;overflow:hidden;
       width:min(95vw,680px);max-height:90vh;display:flex;flex-direction:column;">
    <div style="display:flex;align-items:center;justify-content:space-between;
         padding:12px 16px;background:#f4f4f4;border-bottom:1px solid #e0e0e0;">
      <span style="font-size:15px;font-weight:700;color:#5a7a82;">그림판으로 입력</span>
      <button onclick="closeDrawingPadModal()">✕</button>
    </div>
    <iframe id="drawingPadFrame" src="" style="flex:1;border:none;min-height:480px;"></iframe>
    <div style="padding:12px 16px;display:flex;justify-content:flex-end;gap:8px;">
      <button onclick="closeDrawingPadModal()">취소</button>
      <button onclick="sendDrawingToAnalyze()">이 그림으로 분석하기</button>
    </div>
  </div>
</div>
```

### 그림판 JavaScript 함수 3개

```javascript
function openDrawingPadModal(){
  var m=document.getElementById('drawingPadModal');
  if(!m)return;
  var f=document.getElementById('drawingPadFrame');
  // 최초 열 때만 src 설정 (lazy load)
  if(f && !f.src) f.src='./drawing-pad.html';
  m.style.display='flex';
}

function closeDrawingPadModal(){
  var m=document.getElementById('drawingPadModal');
  if(m) m.style.display='none';
}

function sendDrawingToAnalyze(){
  var f=document.getElementById('drawingPadFrame');
  if(!f||!f.contentWindow){closeDrawingPadModal();return;}
  try{
    var FC=f.contentWindow.FC;
    if(!FC){
      alert('그림판이 아직 로딩 중입니다. 잠시 후 다시 시도하세요.');
      return;
    }
    var d=FC.toDataURL({format:'png',multiplier:2});
    closeDrawingPadModal();
    window._openImageEditor(d, function(ed){ processOcrFromDataUrl(ed); });
  }catch(e){
    closeDrawingPadModal();
    console.error('그림판 캡처 오류', e);
  }
}
```

### 그림판 UI 버튼

```html
<button class="btn-secondary" onclick="openDrawingPadModal()"
        title="그림판으로 입력"
        style="display:inline-flex;align-items:center;gap:5px;">
  <svg width="15" height="15" viewBox="0 0 24 24" fill="none"
       stroke="currentColor" stroke-width="1.5"
       stroke-linecap="round" stroke-linejoin="round">
    <path d="M12 20h9"/>
    <path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4 12.5-12.5z"/>
  </svg>
  그림판
</button>
```

### 학생 등록 코드 50개

```javascript
const STUDENT_ENROLLMENT_CODES = [
  "2EP9TA","2MG4PQ","2WWNZG","3YM3B3","4KZVPT",
  "4TKS2B","5HA2WV","5QMRGL","64U5YL","6EHNP4",
  "7FUQTX","8SRWBW","98U765","9R4STZ","AL7ETW",
  "CB4E3J","CCTNUX","CGUWRL","CL2S5C","CQDLBV",
  "D4G8QG","D6CBF9","DFU8EV","EU9JNT","EUY22K",
  "FHP879","GP66QG","HH5T75","JCNAXF","JEDVYB",
  "JUQTXC","KV5UBB","MCNZ3G","P9PXN6","QGFFTG",
  "QWFEQ5","RW9WSA","SBAG7P","T7BG8M","UQVQFY",
  "UWGWNQ","V68PPZ","VT6MZZ","WZVRYW","XLQRCU",
  "Y9CD2W","YQV5KP","YUCX4J","YUKY2T","ZWLF25"
];
```

---

## 작업 타임라인

1. **문제 발견** — GitHub 리포 파일로 작업 시 버튼 미작동. applyTheme 등 핵심 함수 없음 확인.
2. **2차 문제** — push 후 JS 코드가 텍스트로 렌더링. 모든 탭 하단에 raw JS 노출.
3. **원본 발견** — `deploy/app.html` 발견. Netlify 원본(901,111자). 기준점으로 채택.
4. **폴더 전략 확정** — yeon-ai-kr-v3 동결, yeon-ai-kr-v4 작업본 분리.
5. **등록 코드 추가** — STUDENT_ENROLLMENT_CODES 50개 삽입.
6. **그림판 모달 추가** — drawingPadModal HTML을 `<body>` 직후에 삽입. iframe lazy loading.
7. **그림판 함수 추가** — 3개 함수 추가. Fabric.js FC.toDataURL()로 캔버스 캡처.
8. **그림판 버튼 추가** — 분석하기/샘플/지우기 그룹 뒤에 그림판 버튼 추가.
9. **현재 상태** — 배포 대기 중. 사용자 로컬 확인 후 push 예정.

---

## 함수 검증 결과

- [x] `applyTheme()` — 테마 적용 ✅ 원본 보존
- [x] `resetSettings()` — 설정 초기화 ✅ 원본 보존
- [x] `printShareCard()` — 공유카드 인쇄 ✅ 원본 보존
- [x] `openSettingsModal()` — 설정 모달 열기 ✅ 원본 보존
- [x] `openDrawingPadModal()` — 그림판 열기 ✅ 신규 추가
- [x] `closeDrawingPadModal()` — 그림판 닫기 ✅ 신규 추가
- [x] `sendDrawingToAnalyze()` — 그림 분석 전송 ✅ 신규 추가
- [x] `STUDENT_ENROLLMENT_CODES` — 50개 등록 코드 ✅ 신규 추가
- [x] 그림판 UI 버튼 배치 ✅ 신규 추가
- [x] unescaped `</script>` 없음 ✅ 버그 없음

---

## 배포 명령

```powershell
cd "C:\Users\jinwo\OneDrive\문서\Claude\Projects\연라이프 홈페이지 제작\yeon-ai-kr"

# v4 파일을 배포 폴더로 복사
copy "yeon-ai-kr-v4\yeongpul\yeong-pul.html" "yeongpul\yeong-pul.html"

# git 커밋 & push
git add yeongpul/yeong-pul.html
git commit -m "영풀: deploy/app.html 원본 기반 재구성 + 그림판 + 등록코드"
git push origin main
```

> Service Worker가 `network-first` 전략이므로 push 후 캐시 무효화 없이 바로 반영됩니다.

---

*영풀 파일 작업기록 · 연라이프 프로젝트 · 2026-04-25*
