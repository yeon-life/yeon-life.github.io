# Claude AI Design 대화창에 붙여넣을 메시지

> 사용법: Claude AI Design 세션으로 돌아가서 아래 **【복사할 본문】** 박스 안의 글을
> 통째로 복사해서 대화창에 붙여넣으세요. 한 번에 다 받아올 수 있도록 정리했습니다.

---

## 【복사할 본문】 ↓↓↓ 여기부터 끝까지 복사 ↓↓↓

안녕! 지금까지 만든 연라이프(y-life.kr) 리디자인을 운영 사이트에 실제로 올리려고 해.
다음 세션에서 다른 Claude가 조립할 수 있게, 누락 없이 자료를 한 번 더 정리해서 줘.

**1순위 — 가장 중요: CSS 전체**
지금까지 만든 `yl-*` 클래스 전체에 대한 CSS를 빠짐없이 한 코드 블록으로 줘.
새벽·신문·현대·야간 4종 톤(`data-variant`) 변형, `--text-scale` 변수, 미니 생각지도/메가메뉴/스크랩북/설정 패널/챗봇 등 모든 컴포넌트 스타일이 포함되어야 해.
가능하면 외부 폰트 import(Noto Serif KR + Pretendard) 줄도 같이.

**2순위 — 진입점 HTML**
React + Babel + 7개 JSX 모듈을 모두 로드해서 화면이 실제로 뜨는
**완성된 단일 index.html** 을 줘. 라우팅·상태관리·패널 토글까지 들어간 상위 App 컴포넌트도 포함.
(이전에 export 받은 JSX 7개는 이미 가지고 있어 — YLHome.jsx, YLHeader.jsx, YLArticle.jsx, YLChatbot.jsx, YLScrapbook.jsx, YLSettings.jsx, tweaks-panel.jsx)

**3순위 — 누락된 모듈/자원**
혹시 내가 export 받지 못한 컴포넌트나 데이터 파일이 있다면 같이 알려줘.
예: 라우터 모듈, 공용 유틸, 폰트/아이콘/이미지 에셋, mock data JSON 등.

**전달 방식**
- 가능하다면 zip 다운로드 한 번에. 안 되면 코드 블록으로 차례로.
- CSS는 절대 일부만 주지 말고, 처음부터 끝까지 한 덩어리로.

**왜 필요한가**
지금 가지고 있는 JSX 7개에는 className만 적혀 있고 CSS는 어디에도 없어서,
운영 사이트에 그대로 올리면 화면이 깨져 보여. CSS와 index.html이 있어야 조립이 가능해.
이게 있으면 다음 세션 작업이 반나절로 끝나고, 없으면 처음부터 다시 짜야 해서 며칠 걸려.

부탁할게. 고마워!

## 【복사할 본문】 ↑↑↑ 여기까지 복사 ↑↑↑

---

## 받아온 자료 다음에 어떻게?

1. Claude AI Design에서 받은 CSS·HTML·zip 등을 **이 워크스페이스 폴더에 저장**
   (또는 다음 세션 첫 메시지에 첨부)
2. 다음 세션 첫 메시지로:
   > "Claude AI Design에서 자료 받아왔어. 조립 시작하자"
3. 그 다음은 제가 알아서 Phase 1(조립) → Phase 2(콘텐츠 정리) → Phase 3(기능 보완) → Phase 4(배포) 순서로 진행할게요.

---

## 만약 자료를 못 받아왔을 경우 (한도 또 걸리거나 세션이 닫혔을 때)

다음 세션 첫 메시지로:
> "CSS 못 받아왔어. 새로 짜자"

이라고만 보내시면 됩니다. 그러면 yeon-design 스킬과 새벽의신뢰 팔레트, 스크린샷 39장을 기반으로
CSS를 처음부터 작성합니다. (시간은 더 걸리지만 가능)
