# 연라이프 리디자인 검토 체크리스트

> 작성일: 2026-05-15
> Claude AI Design에서 가져온 7개 JSX 모듈 검토 결과
> 다음 세션에서 이 문서를 먼저 읽으면 바로 작업 재개 가능

---

## 0. 가져온 것 — 한눈에

Claude AI Design 세션에서 export된 **y-life.kr 리디자인 시안**.
추가 기능이 아니라 현재 운영 사이트(yeon-life.github.io/index.html, 5,969줄)를
통째로 교체하는 새 버전.

| 파일 | 역할 | 줄수 | 비고 |
|---|---|---|---|
| YLHome.jsx | Hero/Plaza/Blog/Footer + 광장·칼럼·블로그·연마을 페이지, 영상 큐레이션, 미니 생각지도 | 936 | 가장 큰 모듈 |
| YLHeader.jsx | TopBar/MastHead/MegaMenu, 5대 카테고리 정의 | 394 | MENU_DATA가 여기 |
| YLArticle.jsx | 광장 글 상세 (댓글·사이드바) | 171 | ARTICLE_BODY 하드코딩 |
| YLChatbot.jsx | '연이' 챗봇 (window.claude.complete) | 185 | 챗봇용 컨텍스트 |
| YLScrapbook.jsx | 스크랩 ✂ + MD/TXT 내보내기 (localStorage) | 142 | |
| YLSettings.jsx | 4종 톤·글씨크기·코너순서 (localStorage) | 125 | |
| tweaks-panel.jsx | **Claude AI Design 내부 개발 도구** | 568 | **배포 시 제외** |

스크린샷 39장: 다양한 UI 상태 캡처. 시각 레퍼런스로 보존.
업로드 이미지 3장: 디자인 의뢰 시 참고한 외부 레퍼런스.

---

## 1. 🔴 치명적 누락 — 이대로는 화면이 안 뜸

### 1-1. CSS 파일이 통째로 없음
- 컴포넌트 전체가 `yl-hero`, `yl-plaza-card`, `yl-mega-menu` 같은 `yl-*` 클래스에 의존
- 워크스페이스에 `yl-*` 클래스 정의된 CSS가 **단 한 곳도 없음** (단계4 가이드 일부 조각 제외)
- 색 팔레트는 `디자인시스템_새벽의신뢰.html`에 토큰으로 정의됨 — 활용 가능
- **해결책**:
  - 길 A: Claude AI Design에서 export된 HTML/CSS를 다시 받아오기
  - 길 B: JSX classname 추출 → 새 CSS 작성 (세션 3~5회 소요)

### 1-2. index.html 진입점 없음
- React + Babel CDN, ReactDOM.render, 라우팅, props 전달을 처리하는 wrapper HTML 부재
- 7개 모듈을 하나로 묶을 App 컴포넌트도 없음
- onScrapbookClick, onNavigate, addScrap, activeSection 등 prop은 받지만 누가 내려주는지 알 수 없음

### 1-3. tweaks-panel.jsx 제외 필요
- 주석 명시: `Tweaks shell · host protocol (__activate_edit_mode / __deactivate_edit_mode)`
- Claude AI Design 안에서만 의미 있는 디자인 조정 패널
- 운영 사이트에 들어가면 안 됨

---

## 2. 🟠 콘텐츠 정리 — 가장 손이 많이 가는 곳

### 2-1. 이번 주 화두가 6곳에 흩어져 하드코딩
| 위치 | 변수/요소 |
|---|---|
| YLHome.jsx | `HERO_MAIN` |
| YLHeader.jsx | `MENU_DATA[0].featured` |
| YLArticle.jsx | `HERO_MAIN_FALLBACK` |
| YLChatbot.jsx | `YL_CONTEXT` |
| YLHome.jsx (GwangjangPage) | `yl-hwadu-quote` |
| YLHome.jsx (YLFooter motto) | "연라이프는 천천히 자라는 마을입니다" |

→ **단일 데이터 파일로 분리 필요**. 프로젝트 지시서의 화두 워크플로우와 직결.
→ 추천: `data/current-hwadu.json` 또는 `window.YL_HWADU` 전역.

### 2-2. 가짜 인물·통계 정리
- 가공 인물: 임선생, 박주민, 민서, 하은이, 최영재, 현지영
- 가짜 통계: "오늘 새 글 86", "활동 중인 이웃 248명", "진행 중 토론 12", "구독자 184명"
- 가짜 시간: "2시간 전", "어제", "5월 12일"
- 화두 콘텐츠 자체(Libet 실험·임선생)는 잘 다듬어졌으므로 첫 화두로 그대로 사용 가능

### 2-3. 빈 상태 모순
- "아직 마을에 블로그가 없습니다" 헤드라인 ↔ 같은 화면에 BLOG_OTHERS 4개 카드
- 광장 통계 0이 정답 ↔ 86/248/12 노출
- 연마을 페이지의 "아직 마을 사이트가 없습니다 + 첫 번째 공간을 열어 주세요"는 일관됨 ✓

### 2-4. 날짜 고정
- `new Date(2026, 4, 15)` 가 YLHeader.jsx 두 곳, YLArticle.jsx 하드코딩
- → `new Date()`로 교체

### 2-5. 챗봇 컨텍스트의 가공 인물
- `YL_CONTEXT`에 "임선생·박주민·민서·하은이"가 실제 회원처럼 정의됨
- 사용자가 "민서가 누구예요?"라고 물으면 챗봇이 거짓 사실을 답함
- → 진짜 회원 정보로 교체하거나 인물 부분 삭제

---

## 3. 🟡 기능적 이슈

- [ ] 모든 링크가 `href="#"` — 작동 안 함 (메뉴, 푸터, 전체보기 등)
- [ ] 상위 App 컴포넌트 부재 — 라우터, 상태 관리자 필요
- [ ] CURATED_VIDEOS의 YouTube ID 5개 실제 매핑 검증 필요
- [ ] localStorage 사용 (스크랩·설정) — 정적 사이트면 OK, 로그인 도입 시 서버 동기화 필요
- [ ] 한자 1글자 아바타(蓮·林·朴…) → 사진 업로드되면 충돌 → 폴백 로직
- [ ] 카드 우상단 ✂ 스크랩 버튼 모바일에서 항상 노출 → 시각 노이즈
- [ ] "로그인하면 맞춤 추천이 표시됩니다" 문구 → 로그인 기능 없으면 빈 약속

---

## 4. 🟢 좋은 점 — 그대로 살릴 것

- **MiniThinkMap Canvas 애니메이션** — 광장 생각 연결 시각화. 아이디어 좋음.
- **4종 화면 톤 + 4단 글씨 크기** — 노안 독자 친화적, 접근성 ↑
- **스크랩 노트 → MD/TXT 내보내기** — "여기서 쓴 모든 것은 당신의 자산" 정신과 부합
- **메가메뉴 3-column** (카테고리/하위/지금 주목) — 깔끔한 정보 구조
- **임선생 Libet 화두** — 콘텐츠 자체는 그대로 첫 화두로 사용 가능
- **푸터 모토 "연라이프는 천천히 자라는 마을입니다"** — 정서 잘 살림

---

## 5. 작업 단계 (제안)

### Phase 1 — 화면을 일단 띄우기 (먼저)
- [ ] CSS 입수 (Claude AI Design에서 추가 export) **또는** 새로 작성
- [ ] index.html 진입점 작성 — React + Babel CDN, 모듈 로드, App 마운트
- [ ] 7개 모듈 → 6개로 (tweaks-panel 제외)
- [ ] App 컴포넌트 (라우팅 + 상태 + 패널 토글)
- [ ] 로컬에서 화면 확인

### Phase 2 — 콘텐츠 단일 소스화
- [ ] `data/hwadu.json` — 이번 주 화두 + 보관함 구조 설계
- [ ] `data/site.json` — 통계·메뉴·푸터 등 사이트 상수
- [ ] 모든 가짜 데이터 → `data/` 참조로 변경
- [ ] 가공 인물 제거 또는 실제 회원으로 교체

### Phase 3 — 기능 보완
- [ ] 모든 `href="#"` 실제 경로/스크롤로 교체
- [ ] 날짜 동적화 (`new Date()`)
- [ ] 빈 상태 UI 솔직하게 (가짜 숫자 0으로 또는 숨김)
- [ ] YouTube 영상 ID 검증
- [ ] 아바타 시스템 (사진 + 한자 폴백)
- [ ] 챗봇 컨텍스트 정리

### Phase 4 — 배포
- [ ] 현재 yeon-life.github.io/index.html 백업 (이미 `index_backup_before_length_fix.html` 있음)
- [ ] 새 버전 푸시
- [ ] 모바일/데스크탑 양쪽 점검
- [ ] 4종 톤 모두 점검

---

## 6. 다음 세션 시작 방법

다음 세션 첫 메시지로 다음 중 하나를 보내시면 됩니다:

- **CSS 받아오셨다면**: "Claude AI Design에서 CSS 가져왔어. (붙여넣기 또는 파일 업로드)"
- **못 받아오셨다면**: "체크리스트 보고 Phase 1부터 시작하자"
- **다른 부분부터**: "Phase 2 콘텐츠 정리부터 하자" 등

이 파일 위치:
`C:\Users\jinwo\OneDrive\문서\Claude\Projects\연라이프 홈페이지 제작\연라이프_리디자인_검토_체크리스트.md`

가져온 원본 파일 위치(자동 정리 전):
`outputs/yeon-import/` (Claude 임시 작업 공간 — 세션 종료 시 사라짐)

원본 zip 보존 위치:
업로드 폴더에 있는 `연라이프 홈페이지.zip` — 워크스페이스로 옮길지 결정 필요

---

## ★ 연소사 본인이 다음에 할 일 ★

순서대로 처리하시면 됩니다.

### ① (가장 중요) Claude AI Design에서 CSS 가져오기
한도가 풀리면 Claude AI Design 세션으로 돌아가서 다음 중 하나를 시도:
- **"Export as HTML"** 또는 **"Download HTML"** 버튼이 있는지 확인 — 있으면 클릭해서 완성된 HTML 받기
- 또는 미리보기 화면에서 **개발자 도구(F12) → Sources/Elements 탭**에서 `<style>` 태그나 외부 CSS 파일 내용을 복사
- 또는 그냥 **Claude에게 "이 디자인의 CSS 전체를 텍스트로 줘"** 라고 요청
- 받은 CSS/HTML은 zip이든 텍스트든 다음 세션에 업로드해 주세요

> 이게 있으면 작업이 **반나절**로 끝나고, 없으면 **세션 3~5회** 걸립니다. 무조건 받아오시는 게 이득입니다.

### ② 원본 zip 백업 결정
`연라이프 홈페이지.zip`은 현재 임시 업로드 폴더에 있어서 세션 종료 시 사라집니다.
- 본인 PC 어딘가에 따로 보관하고 계신다면 OK
- 아니면 다음 세션에서 "워크스페이스로 옮겨줘" 한마디만 해 주세요

### ③ 콘텐츠 정책 결정 (다음 세션 전에 생각해 두기)
다음을 미리 정해두시면 Phase 2 작업이 빨라집니다:
- 가공 인물(임선생·박주민·민서·하은이) 자리를 **실제 회원으로 채울지** vs **다 비우고 시작할지**
- 첫 화두로 **Libet 실험(임선생) 그대로** 갈지 vs **새 화두를 생성**할지
- 이번 주 가짜 통계(248명·86글) 자리를 **0으로 솔직하게** 갈지 vs **숨길지**

### ④ 다음 세션 시작 방법
첫 메시지를 다음 중 하나로:
- **"CSS 받아왔어. 여기 있어 [파일/텍스트]"** → 바로 조립 시작
- **"CSS 못 받아왔어. Phase 1부터 새로 짜자"** → 새 CSS 작성 시작
- **"화두 먼저 다듬자"** → 콘텐츠 정리부터
- **"체크리스트 다시 읽어줘"** → 이 문서 다시 검토

이 체크리스트 파일은 워크스페이스에 영구 보존되니, 언제든 열어 보실 수 있습니다.
