# 연라이프 홈페이지 · 아침에 이어서 할 일

> 어제(2026-04-19) 밤 업로드까지 진행했으나 "뭐 안 된다" 상태에서 멈춤.
> 이 순서대로만 따라가시면 됩니다.

---

## 1단계 · 먼저 업로드가 제대로 됐는지 확인

### 해볼 것

1. 크롬에서 아래 주소로 이동
   ```
   https://github.com/yeon-life/yeon-life.github.io
   ```
2. 화면 가운데에 파일 목록이 보이면 업로드는 일단 성공
3. 그 목록에 다음이 **폴더째로** 보이는지 확인:
   - `.github/` 폴더
   - `assets/` 폴더
   - `css/` 폴더
   - `js/` 폴더
   - `locales/` 폴더
   - `saranbang-widget/` 폴더
   - `index.html`, `writer-yeonsosa.html`, `writers.html`, `student.html`, `parent.html`, `news.html`, `column.html`, `question-forest.html`
   - `README.md`, `LICENSE`

### 결과별 분기

- **A · 폴더와 파일이 위처럼 다 보이면** → 2단계(Pages 활성화)로 이동
- **B · `index.html`은 있는데 폴더가 없고 파일들이 평평하게 있으면** → 3단계(재업로드)로 이동
- **C · 리포가 비어 있으면** (업로드 안 된 상태) → 3단계(재업로드)로 이동

---

## 2단계 · GitHub Pages 활성화 (업로드 잘 된 경우)

1. 리포 상단 메뉴에서 **"Settings"** (톱니바퀴 아이콘) 클릭
2. 왼쪽 사이드바에서 **"Pages"** 클릭 (Code and automation 섹션 아래)
3. **Source** 항목에서:
   - **"Deploy from a branch"** 선택
   - Branch: **main** / **/ (root)** 선택
   - **Save** 클릭
4. 위쪽에 파란색 안내가 뜸 — "Your site is live at https://yeon-life.github.io/"
5. **1~3분 기다린 뒤** 주소창에 `https://yeon-life.github.io/` 입력해서 접속
6. 홈페이지가 보이면 성공 ✅

> 처음엔 404가 뜰 수 있어요. 5분 기다렸다가 다시 해보세요.

---

## 3단계 · 재업로드 (파일 구조가 깨진 경우)

### 먼저 기존 파일 지우기 (선택)

평평하게 올라간 파일이 있으면 덮어쓰지 말고 지우는 게 깔끔합니다.

1. 리포 페이지에서 잘못 올라간 파일 하나 클릭
2. 파일 화면 오른쪽 위 **휴지통 아이콘** 클릭
3. 맨 아래 **Commit changes** 클릭
4. 각 파일마다 반복

또는 간단하게 리포를 지우고 다시 만들기도 가능:
- Settings → 맨 아래 **Delete this repository** → `yeon-life/yeon-life.github.io` 입력 → 삭제
- 다시 Public으로 `yeon-life.github.io` 이름으로 생성

### 재업로드 (GitHub Desktop 권장 · 가장 확실한 방법)

웹 드래그-드롭은 큰 폴더에서 자주 실패해요. **앱**으로 하는 게 100배 안정적입니다.

1. https://desktop.github.com/ 접속 → **Download for Windows** 클릭 → 설치
2. 앱 실행 → **Sign in to GitHub.com** → 연소사님 계정으로 로그인
3. 상단 메뉴 **File → Clone repository**
4. 탭에서 **GitHub.com** 선택
5. 리포 목록에서 `yeon-life/yeon-life.github.io` 선택
6. Local path: 기본 경로(`C:\Users\jinwo\Documents\GitHub\`) 그대로 두고 **Clone**
7. 잠시 기다리면 빈 폴더가 만들어져 있음
8. 탐색기에서 그 폴더 열기 (`C:\Users\jinwo\Documents\GitHub\yeon-life.github.io\`)
9. 컴퓨터의 `yeonlife_GitHub배포팩\1단계_GitHub에_올릴_파일\` 폴더 안의 **모든 내용물**(폴더+파일)을 위 빈 폴더로 **복사 붙여넣기**
10. GitHub Desktop 앱으로 돌아오면 왼쪽에 변경된 파일 목록이 우르르 뜸
11. 맨 아래 **Summary**: `초기 홈페이지 업로드` 입력
12. **Commit to main** 클릭
13. 상단의 **Push origin** 클릭
14. 완료 → 웹 브라우저에서 리포 다시 열어보면 파일 올라가 있음
15. 그 다음 **2단계**(Pages 활성화) 진행

---

## 4단계 · 최종 확인

`https://yeon-life.github.io/` 접속해서:
- [ ] 홈페이지가 뜬다
- [ ] 디자인이 깨지지 않았다 (폴더 구조 OK)
- [ ] 언어 전환(한/영/중/일 등)이 동작한다
- [ ] 각 페이지 링크(student, parent, writers 등)가 클릭된다

문제 있으면 Claude에게 "4단계 확인했는데 OO가 안 돼" 라고만 말씀주시면 이어서 도와드릴게요.

---

## 참고 · 업로드 주소

- 리포: `https://github.com/yeon-life/yeon-life.github.io`
- 업로드: `https://github.com/yeon-life/yeon-life.github.io/upload/main`
- 설정(Pages): `https://github.com/yeon-life/yeon-life.github.io/settings/pages`
- 최종 홈페이지: `https://yeon-life.github.io/`
